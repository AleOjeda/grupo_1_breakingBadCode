<!DOCTYPE html>
<html lang="es">

<%- include('./partials/head') %>

<body>
    <header>
    <%- include('partials/header') %>
    <%- include('partials/categoriesCarouselDesktop') %>
    <%- include('partials/categoriesCarousel') %>
    </header>
    <div class="container">

        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
              <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
              <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
              <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
            </ol>
            <div class="carousel-inner">
              <div class="carousel-item active">
                <img src="http://www.heurisko.ar/otros/slide_1.png" class="d-block w-100" alt="...">
              </div>
              <div class="carousel-item">
                <img src="http://www.heurisko.ar/otros/slide_2.png" class="d-block w-100" alt="...">
              </div>
              <div class="carousel-item">
                <img src="http://www.heurisko.ar/otros/slide_3.png" class="d-block w-100" alt="...">
              </div>
            </div>
            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="sr-only">Next</span>
            </a>
          </div>

          <div class="bienvenida-home">
              <h1>TU SUPERMERCADO AHORA MAS CERCA</h1>
              <h4>Bienvenido a la tienda online en donde podrás encontrar todo lo que hay en un supermercado, hipermercado, almacen de barrio o los chino.</h4>
          </div>

          <div class="como-comprar">
              <img class="imagen-como-comprar" src="http://www.heurisko.ar/otros/como_comprar.png" alt="">
          </div>

        <!-- <div id="home-catalogo">
            <% categories.forEach(category => { %>
            <div id="catalogo-categoria">
                <div class="catalogo-header">
                    <div class="catalogo-header-titulo"><%= category.category%></div>
                    <a class="catalogo-header-vertodo" href="/categorias/<%=category.path%>"><div class="">Ver todo ></div></a>
                </div>
                <div class="catalogo-contenedor">
                    <% category.products.forEach(product => { %>
                    <div class="producto-contenedor margen-derecho">
                        <div class="producto-contenedor-secundario">
                            <div class="producto-header">
                                <div class="producto-descuento <%= product.discount === '0%' ? 'ocultar' : '' %>"><%= product.discount%></div>
                                <div class="producto-favorito"><i class="fas fa-heart"></i></div>
                            </div>
                            <div class="producto-imagen">
                                <img src= <%= product.image%>>
                            </div>
                            <div class="producto-footer">
                                <div class="producto-titulo">
                                    <p class="producto-detalle"> <%= product.description%>
                                    <span class="producto-presentacion"> x <%= product.display%></span>
                                    </p>
                                </div>
                                <div class="producto-precios">
                                    <span class="precio-nuestro"> <%= product.price%>
                                    <span class="precio-competencia <%= product.discount === '0%' ? 'ocultar' : '' %>"> <%= product.oldPrice%> </span>
                                    </span>
                                </div>
                                <div id="botonera">
                                    <div id="botones-superiorSKU=<%= product.id%>" class="ocultar">
                                        <button onclick="addSubtractRemoveQuantity(<%= product.id%>,'subtract')" id="subtractOneSku=<%= product.id%>" class="boton-sumarrestar ">-</button >
                                        <span id="quantitySku=<%= product.id%>" class="quantityProduct">0</span>
                                        <button onclick="addSubtractRemoveQuantity(<%= product.id%>,'add')" id="addOneSku=<%= product.id%>" class="boton-sumarrestar ">+</button >
                                    </div>
                                    <div id="botones-inferiorSKU=<%= product.id%>" class="mostrar" >
                                        <div onclick="addSubtractRemoveQuantity(<%= product.id%>,'create')" id="addCartSku=<%= product.id%>" class="boton-agregar">Agregar</div >
                                    </div>
                                    </div>
                            </div>
                        </div>
                    </div>
                    <% });%>
                </div>
            </div>
            <% });%>
        </div> -->
    </div>
    <script> document.addEventListener('gesturestart', function (e) {
        e.preventDefault();
    });></script>
    <script> document.addEventListener('touchmove', function(event) {
        event = event.originalEvent || event;
        if(event.scale > 1) {
          event.preventDefault();
        }
      }, false);</script>
</body>

</html>